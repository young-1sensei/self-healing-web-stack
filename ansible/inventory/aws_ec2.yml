plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:Type: Managed-Webserver
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address
groups:
  webservers: "tags.Type == 'Managed-Webserver'"
